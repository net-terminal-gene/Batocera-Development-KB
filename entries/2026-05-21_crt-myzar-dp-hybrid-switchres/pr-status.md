# PR Status — Myzar DisplayPort Hybrid Switchres

## No PR yet

Configuration and Cursor skills only (`~/.cursor/skills/myzar-dp/`). Nothing merged to Batocera-CRT-Script or batocera.linux.

If upstreaming later, likely targets:

- Documented hybrid pattern for DP-only RDNA4 + `display.rotate=1`
- Optional: installer hook for `custom.sh` symlink block (v43 `custom_service`)
