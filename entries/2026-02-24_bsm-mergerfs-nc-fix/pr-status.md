# PR Status — batocera-storage-manager mergerFS =NC Fix

## Merged to batocera.linux

Resolved via batocera.linux merge. `=NC` (No Create) branch policy now applied to BASE_DIR to prevent silent ROM duplication on NVMe.
